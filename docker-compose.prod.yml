services:
  storage:
    image: redis
    volumes: 
      - ./data:/data
  library:
    image: aimenin197/library:0.0.2
    environment:
      - REDIS_URL=redis://storage
    ports:
      - 3000:3000
    depends_on: 
      - storage